# ============================================================================
# CHATTERBOX TTS SERVER - REQUIREMENTS (CPU-ONLY INSTALLATION)
# ============================================================================
# This file ensures compatible PyTorch, ML, and audio processing libraries
# are installed for CPU-only operation. All packages are pinned for stability.

# --- PyTorch CPU-Only Installation Index ---
# Use PyTorch's official CPU-only package index for torch components
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Deep Learning Framework (CPU-Only) ---
# Updated to match chatterbox-tts requirement for torch==2.7.1
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# --- Core Application Dependencies ---

# Chatterbox TTS engine - Install from chatterbox-v2 fork
chatterbox-tts @ file:./src/chatterbox-main
# Core Web Framework
fastapi
uvicorn[standard]

# Other ML & Audio Libraries
numpy>=1.24.0,<1.26.0  # Match chatterbox-v2 requirements
onnxruntime==1.16.3
soundfile  # Requires libsndfile system library
librosa
safetensors
descript-audio-codec

# Configuration & Utilities
PyYAML
python-multipart
requests
Jinja2
watchdog
aiofiles
unidecode
inflect
tqdm
hf_transfer                     # Speed up file transfers

# Audio Post-processing
pydub
audiotsm
praat-parselmouth

russtress==0.1.4
ruaccent
chardet

# ui/presets.yaml
# Predefined examples for the TTS User Interface.
# These presets are designed for a single-speaker TTS engine and provide
# varied texts for testing quality, intonation, and chunking.
# Each preset has a name, the text to synthesize, and 'params' for generation settings.

- name: "Standard Narration"
  params:
    temperature: 0.8
    exaggeration: 0.4 # Neutral exaggeration
    cfg_weight: 0.5
    seed: 42
    speed_factor: 1.0

- name: "Expressive Monologue"
  params:
    temperature: 0.75
    exaggeration: 1.1 # Higher exaggeration for dramatic effect
    cfg_weight: 0.6
    seed: 101
    speed_factor: 1.0

- name: "Technical Explanation"
  params:
    temperature: 0.85
    exaggeration: 0.4 # Lower exaggeration for clarity
    cfg_weight: 0.5
    seed: 2024
    speed_factor: 1.0

- name: "Upbeat Advertisement"
  params:
    temperature: 0.8
    exaggeration: 1.3 # More energy
    cfg_weight: 0.45
    seed: 3000
    speed_factor: 1.0

- name: "Thoughtful Reflection (Chunking Test)"
  params:
    temperature: 0.7
    exaggeration: 0.4
    cfg_weight: 0.6
    seed: 555
    speed_factor: 1.0

- name: "Simple Punctuation Test"
  params:
    temperature: 0.8
    exaggeration: 0.5
    cfg_weight: 0.5
    seed: 1212
    speed_factor: 1.0

- name: "Long Story Excerpt (Chunking Test)"
  params:
    temperature: 0.78
    exaggeration: 1.1
    cfg_weight: 0.55
    seed: 1775
    speed_factor: 1.0
